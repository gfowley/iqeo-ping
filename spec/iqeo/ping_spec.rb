require 'spec_helper'

describe Iqeo::Ping do
  it 'has a version number' do
    expect(Iqeo::Ping::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
